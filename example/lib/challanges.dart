import 'package:flutter/material.dart';

class ChallangesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Challanges'),
      ),
    );
  }
}